
function isoddmodr(x)
%This function checks if a given array x is a odd mod r signal
%The main period of the function is considered as the input x
%ie. the length of x is essentially r, the period
    r = length(x);
    for n=1:len
    if 
       (r/gcd())
    end
    end
end